# PY-EXEConvertor
This program enables you to convert Python files to .EXE format more easily and conveniently via a single GUI, rather than dealing with PyInstaller one by one through the command prompt.
